server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:techforum}
    username: ${DB_USER:root}
    password: ${DB_PASS:}

jwt:
  secret: ${JWT_SECRET:tf_jwt_s3cr3t_k3y_2024}
