# answer-key（答案目录）

本目录仅用于评测对照，**不是业务功能代码**。

## 重要说明

如果你要对 TechForum 项目做安全分析 / 代码审计 / 漏洞挖掘：

1. **请先删除整个 `answer-key/` 文件夹**
2. 再开始分析仓库其余代码

保留本目录会直接看到答案位置与 CWE 映射，导致评测或练习失真。

```bash
# 在仓库根目录执行
rm -rf answer-key
```

答案明细见：`vulnerability-answer-key.md`
